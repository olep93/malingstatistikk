"use client";
import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { stores, suppliers } from "@/data/demo";
const palette: Record<string,string> = { Infra: "#0068b5", Butinox: "#ed6b23", Jotun: "#24304a" };
export function SupplierChart(){return <ResponsiveContainer width="100%" height={290}><BarChart data={suppliers} margin={{left:0,right:8,top:10,bottom:0}}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name"/><YAxis tickFormatter={v=>`${v/1000}k`}/><Tooltip formatter={(v)=>`${Number(v).toLocaleString("nb-NO")} kr`}/><Bar dataKey="revenue" radius={[8,8,0,0]}>{suppliers.map(s=><Cell key={s.name} fill={palette[s.name]}/>)}</Bar></BarChart></ResponsiveContainer>}
export function ShareChart(){return <ResponsiveContainer width="100%" height={250}><PieChart><Pie data={suppliers} dataKey="share" nameKey="name" innerRadius={62} outerRadius={95} paddingAngle={3}>{suppliers.map(s=><Cell key={s.name} fill={palette[s.name]}/>)}</Pie><Tooltip formatter={(v)=>`${v}%`}/></PieChart></ResponsiveContainer>}
export function StoresChart(){return <ResponsiveContainer width="100%" height={300}><BarChart data={stores} layout="vertical" margin={{left:15,right:20}}><CartesianGrid strokeDasharray="3 3" horizontal={false}/><XAxis type="number" tickFormatter={v=>`${v/1000}k`}/><YAxis type="category" dataKey="store" width={82}/><Tooltip formatter={(v)=>`${Number(v).toLocaleString("nb-NO")} kr`}/><Bar dataKey="revenue" radius={[0,7,7,0]}>{stores.map(s=><Cell key={s.store} fill={s.store==="Tønsberg"?"#0068b5":"#a9b5c4"}/>)}</Bar></BarChart></ResponsiveContainer>}
