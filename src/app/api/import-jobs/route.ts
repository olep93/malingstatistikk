import {NextResponse} from 'next/server';
import {getSession,isAuthenticated} from '@/lib/server/auth';
import {ensureSchema,sql} from '@/lib/server/db';
export async function GET(){if(!(await isAuthenticated()))return NextResponse.json({error:'Ikke innlogget'},{status:401});try{await ensureSchema();const q=sql();const jobs=await q`SELECT id::text,source_name,status,total_days,staged_days,total_products,synced_products,imported_days,failed_products,failed_days,created_by,created_at,updated_at FROM paint_import_jobs ORDER BY created_at DESC LIMIT 25`;return NextResponse.json({jobs})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Kunne ikke hente importjobber'},{status:500})}}
export async function POST(req:Request){const session=await getSession();if(!session)return NextResponse.json({error:'Ikke innlogget'},{status:401});try{await ensureSchema();const {sourceName,totalDays=0}=await req.json();if(!String(sourceName||'').trim())return NextResponse.json({error:'Filnavn kreves'},{status:400});const q=sql();const rows=await q`INSERT INTO paint_import_jobs(source_name,total_days,created_by) VALUES(${String(sourceName).trim()},${Number(totalDays)||0},${session.username}) RETURNING id::text`;return NextResponse.json({ok:true,id:rows[0].id})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Kunne ikke opprette importjobb'},{status:500})}}
