import {NextResponse} from 'next/server';
import {del} from '@vercel/blob';
import {isAdmin,isAuthenticated} from '@/lib/server/auth';
import {ensureSchema,sql} from '@/lib/server/db';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAuthenticated()))return NextResponse.json({error:'Ikke innlogget'},{status:401});try{await ensureSchema();const {id}=await params,q=sql();const jobs=await q`SELECT id::text,source_name,status,total_days,staged_days,total_products,synced_products,imported_days,failed_products,failed_days,created_by,created_at,updated_at,blob_size,analyzed_at FROM paint_import_jobs WHERE id=${id}::bigint`;if(!jobs.length)return NextResponse.json({error:'Importjobben finnes ikke'},{status:404});const days=await q`SELECT report_date::text,status,error FROM paint_import_job_days WHERE job_id=${id}::bigint ORDER BY report_date`;return NextResponse.json({job:jobs[0],days})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Kunne ikke hente importjobb'},{status:500})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAdmin()))return NextResponse.json({error:'Kun Admin kan slette importjobber'},{status:403});try{await ensureSchema();const {id}=await params,q=sql();const rows=await q`SELECT blob_url FROM paint_import_jobs WHERE id=${id}::bigint`;if(rows[0]?.blob_url){try{await del(rows[0].blob_url)}catch{}}await q`DELETE FROM paint_import_jobs WHERE id=${id}::bigint`;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Kunne ikke slette importjobb'},{status:500})}}
